version: '3.8'

services:
  # FastAPI application
  app:
    build: .
    container_name: fastapi_app
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - ./src:/app/src
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app_network
    restart: unless-stopped

  # PostgreSQL database
  db:
    image: postgres:16-alpine
    container_name: postgres_db
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-fastapi_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # React client
  client:
    image: node:20-alpine
    working_dir: /app
    container_name: react_client
    environment:
      - CHOKIDAR_USEPOLLING=1
    command: sh -c "npm install && npm run dev -- --host --port 5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    ports:
      - "${CLIENT_PORT:-5173}:5173"
    depends_on:
      app:
        condition: service_started
    networks:
      - app_network
    restart: unless-stopped

  # Mobile client
  mobile:
    image: node:20-alpine
    working_dir: /app
    container_name: mobile_client
    environment:
      - CHOKIDAR_USEPOLLING=1
    command: sh -c "npm install && npm run dev -- --host --port 5000"
    volumes:
      - ./mobile:/app
      - /app/node_modules
    ports:
      - "${MOBILE_PORT:-5000}:5000"
    depends_on:
      app:
        condition: service_started
    networks:
      - app_network
    restart: unless-stopped

# Named volumes
volumes:
  postgres_data:

# Networks
networks:
  app_network:
    driver: bridge

